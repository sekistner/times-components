import { StyleSheet } from "react-native";
import sharedStyles from "./shared";

const styles = {
  DatawrapperBody: {
    width: "100%"
  },
  DatawrapperWebView: {
    width: "100%",
    border: 0
  }
};

export default styles;
